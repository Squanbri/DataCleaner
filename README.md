# DataCleaner

ASP.NET Core service for cleaning CRM customer exports: normalize phones, names, emails and dates, find duplicates, and report data quality.

## Run

```bash
docker compose up -d
dotnet run --project src/DataCleaner.Api
```

Setup details and usage will grow as the project takes shape.
